# Fixture: must trigger gitleaks' built-in GitHub PAT detector (or generic
# fallback). `ghp_` + 36 alphanumeric per the v8 default rule.
# Synthetic, never issued.
GITHUB_TOKEN = "ghp_FixtureABCDEFGHIJKLMNOPQRSTUVWX0123A"
